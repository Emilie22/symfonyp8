<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

	<a href="{{ path('home') }}">Home</a>
	<a href="{{ path('bonjour') }}">Bonjour</a>
	<a href="{{ path('bonjourNom', {nom: 'emilie'}) }}">BonjourNom</a>
	<a href="{{ path('cava') }}">Comment-allez-vous ?</a>
	<a href="{{ path('heure') }}">Heure</a>
	<a href="{{ path('exo3', {age: 33, pseudo: 'emilie'}) }}">Exo3</a>

	<h1>Page d'accueil</h1>
	<p>Ceci est la page d'accueil du site</p>
	
	{# commentaires twig #}

	{# pour afficher ue variable : #}
	{#
	{{ mavariable }}
	#}

	{# boucle #}
	{#
	{% for article in articles %}

	{% endfor %}
	#}

	{# condition #}
	{#
	{% if mavariable == 'toto' %}

	{% endif %}
	#}

	<p>Bonjour {{nom}}</p>

	<a href="{{ path('bonjour') }}">Lien vers la page bonjour</a>


</body>
</html>